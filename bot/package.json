{
  "name": "bot",
  "version": "1.0.0",
  "description": "A Discord bot to handle keyboard meetup RSVPs",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node --env-file=.env build/index.js",
    "dev": "tsc --watch & node --env-file=.env --watch build/index.js"
  },
  "dependencies": {
    "discord.js": "^14.16.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  }
}
